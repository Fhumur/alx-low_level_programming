#include "main.h"

/**
 * _islower - checks for lowercase
 * @c: - is the character to be checked
 * Return: 1 for success 0 for else
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
